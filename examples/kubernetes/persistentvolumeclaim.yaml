apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: open-stocks-mcp-data
  labels:
    app: open-stocks-mcp
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
