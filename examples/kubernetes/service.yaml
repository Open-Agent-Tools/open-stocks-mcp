apiVersion: v1
kind: Service
metadata:
  name: open-stocks-mcp
  labels:
    app: open-stocks-mcp
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: open-stocks-mcp
